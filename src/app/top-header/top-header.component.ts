import { Component } from '@angular/core';

@Component({
  // selector: '[top-header]', // as an attribute, mostly used as an directive
  // selector: '.top-header', // as a class
  // selector: '#top-header', // as a id
  selector: 'top-header', // as a html tag
  templateUrl: './top-header.component.html',
  styleUrls: ['./top-header.component.css']
})
export class TopHeaderComponent {

}
